Comparison operators:
	'in' and 'in not' check whether a value occurs(does not occur) in a sequence.
	'is' and 'is not' compare whether two objects are really the same object; this only matters for mutable objects like lists.
	'and' and 'or' boolean operators(short-circuit operators)
	
	
